import { Component/* , Input, OnInit */ } from '@angular/core';

@Component({
  selector: 'app-accordion',
  templateUrl: './accordion.component.html',
  styleUrls: ['./accordion.component.css']
})
export class AccordionComponent /* implements OnInit */ {
/*   @Input() id: number = 0;
  @Input() btnStr: string = '';
  @Input() body: string = '';
  public elementId: string = '';
  public elemento: string = '';
  public encabezado: string = '';

  ngOnInit(): void {
    this.elementId = `#elemento-${this.id}`;
    this.elemento = `elemento-${this.id}`;
    this.encabezado = `encabezado-${this.id}`;
    this.btnStr = this.btnStr ? this.btnStr.toString() : '';
    this.body = this.body ? this.body.toString() : '';
  } */
}

 